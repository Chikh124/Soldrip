# ⚡ Quick Start Guide

## 🚀 За 5 хвилин до першого гаманця

### Крок 1: Встановіть Rust (2 хвилини)

**Windows:**
1. Відкрийте https://rustup.rs/
2. Завантажте `rustup-init.exe`
3. Запустіть та слідуйте інструкціям
4. Перезавантажте термінал

**Linux/macOS:**
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env
```

### Крок 2: Запустіть програму (1 хвилина)

**Windows:**
```bash
run.bat
```

**Linux/macOS:**
```bash
chmod +x run.sh
./run.sh
```

### Крок 3: Згенеруйте гаманці (2 хвилини)

```
Оберіть дію: 1
Скільки гаманців згенерувати? 10
```

✅ **Готово!** Ваші гаманці в файлі `wallets.csv`

---

## 📋 Наступні кроки

### 1. Перевірте згенеровані гаманці

```bash
# Windows
type wallets.csv

# Linux/macOS
cat wallets.csv
```

### 2. Підключіть до друга для інтеграції API

Надайте другу файл [`INTEGRATION_GUIDE.md`](INTEGRATION_GUIDE.md)

### 3. Після готовності API - підключіть гаманці

```
Оберіть дію: 2
```

### 4. Запустіть автоматичний claim

```
Оберіть дію: 3
```

---

## 🎯 Швидкий тест (без API)

Хочете просто потестувати генерацію гаманців?

```bash
cargo run --release
# Оберіть "1"
# Введіть "5"
# Перевірте wallets.csv
```

---

## 💡 Типові сценарії

### Сценарій 1: Генерація 1000 гаманців
```
1. Запустити програму
2. Вибрати "1" (Генерувати)
3. Ввести "1000"
4. Дочекатись завершення (~2-3 хвилини)
5. wallets.csv готовий!
```

**Очікуваний результат:**
- 1000 рядків у CSV
- Кожен гаманець з унікальним address та private_key
- Всі гаманці зі статусом "pending"

### Сценарій 2: Масове підключення
```
1. Переконайтесь що API готове
2. Запустити програму
3. Вибрати "2" (Підключити)
4. Дочекатись завершення
```

**Після успішного підключення:**
- Статус змінено на "connected"
- Готові до claim

### Сценарій 3: Щоденний claim
```bash
# Додайте в cron (Linux) або Task Scheduler (Windows)

# Linux cron - щодня о 12:00
0 12 * * * cd /path/to/testdrip && cargo run --release <<< "3"

# Windows Task Scheduler
# Program: cargo
# Arguments: run --release
# Working directory: C:\path\to\testdrip
```

---

## 📊 Розрахунок профіту

| Кількість гаманців | SOL/місяць | USD/місяць (@$100) |
|---------------------|------------|-------------------|
| 10                  | 1.5        | $150              |
| 100                 | 15         | $1,500            |
| 500                 | 75         | $7,500            |
| **1000**            | **150**    | **$15,000**       |
| 5000                | 750        | $75,000           |

*При дрип-рейті 0.15 SOL/місяць на гаманець*

---

## ⚠️ Важливі нагадування

1. **Безпека:**
   - ❌ НЕ діліться `wallets.csv` ні з ким
   - ✅ Робіть бекапи щодня
   - ✅ Використайте шифрування для cloud backup

2. **API готовність:**
   - Спочатку переконайтесь що API готове
   - Зробіть тест з 1-5 гаманцями
   - Тільки потім масово підключайте

3. **Rate limiting:**
   - Не підключайте всі 1000 одразу
   - Використайте затримки (вже є в коді)
   - Слідкуйте за помилками

---

## 🔍 Швидка перевірка

Після генерації перевірте CSV:

```bash
# Порахувати кількість гаманців
# Windows (PowerShell)
(Get-Content wallets.csv | Measure-Object -Line).Lines - 1

# Linux/macOS
wc -l wallets.csv
```

Перевірити формат:
```bash
# Windows
type wallets.csv | Select-Object -First 2

# Linux/macOS
head -n 2 wallets.csv
```

---

## 🆘 Проблеми?

- **Rust не встановився?** → Перезавантажте термінал
- **Компіляція не працює?** → `cargo clean && cargo build --release`
- **API не відповідає?** → Читайте [`TROUBLESHOOTING.md`](TROUBLESHOOTING.md)

---

## 📞 Підтримка

1. Прочитайте [`README.md`](README.md) - повна документація
2. Перевірте [`TROUBLESHOOTING.md`](TROUBLESHOOTING.md) - рішення проблем
3. Для інтеграції API - [`INTEGRATION_GUIDE.md`](INTEGRATION_GUIDE.md)

---

**Готові заробляти SOL? Вперед! 🚀💰**
