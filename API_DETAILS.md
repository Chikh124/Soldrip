# 🔌 Деталі soldrip.io API

## ✅ Підключення гаманця (ГОТОВО!)

**Endpoint:** `POST https://soldrip.io/api/auth/connect`

**Формат запиту:**
```json
{
  "walletAddress": "ваша_адреса",
  "referralCode": null,
  "signature": "підпис_повідомлення",
  "message": "SOLdrip\n\nAction: connect\nWallet: адреса\nTimestamp: мілісекунди\nNonce: 0",
  "timestamp": 1770811150343
}
```

**Headers:**
- `Content-Type: application/json`
- `x-fingerprint: sha256_hash`

**✨ Статус: РЕАЛІЗОВАНО в коді!**

---

## ❓ Claim (ПОТРІБНА ІНФОРМАЦІЯ)

Щоб дізнатися формат claim API, зробіть наступне:

### 1️⃣ Відкрийте soldrip.io в браузері

### 2️⃣ Відкрийте DevTools (F12)

### 3️⃣ Перейдіть на вкладку Network

### 4️⃣ Підключіть гаманець і натисніть "Claim"/"Withdraw"

### 5️⃣ Знайдіть запит до API (шукайте POST запит до /api/...)

### 6️⃣ Клік правою кнопкою → Copy → Copy as cURL (bash)

### 7️⃣ Надішліть curl команду - я оновлю код!

---

## 🔍 Що шукати в Network tab:

Після кліку на Claim/Withdraw, шукайте запити до:
- `/api/auth/claim`
- `/api/wallet/claim`
- `/api/claim`
- Або щось схоже

**Приклад того, що потрібно:**
```bash
curl "https://soldrip.io/api/auth/claim" \
  -H "content-type: application/json" \
  --data-raw '{"walletAddress":"...","signature":"...","message":"...","timestamp":...}'
```

---

## 🧪 Тестування поточної реалізації

Спробуйте підключити 1 гаманець:

```bash
# 1. Згенеруйте 1 тестовий гаманець
cargo run --release
# Оберіть "1", введіть "1"

# 2. Спробуйте підключити
# Оберіть "2"

# 3. Дивіться логи
RUST_LOG=info cargo run --release
```

Якщо підключення працює - побачите:
```
✅ Гаманець успішно підключено!
```

Якщо помилка - побачите деталі помилки.

---

## 📊 Інші корисні endpoints (опціонально)

Якщо є інші endpoints, надішліть curl команди:

### Перевірка балансу:
```bash
GET /api/wallet/balance/{address}
```

### Статистика:
```bash
GET /api/stats
```

### Історія claim:
```bash
GET /api/wallet/history/{address}
```

---

## 🚀 Що працює зараз:

- ✅ Генерація Solana гаманців
- ✅ Збереження в CSV
- ✅ Підключення гаманців до soldrip.io (через API)
- ✅ Підпис повідомлень
- ✅ Fingerprint генерація

## ⏳ Що потрібно додати:

- ❓ Claim функціонал (чекаємо curl від друга)
- ❓ Перевірка балансу (опціонально)

---

**Після отримання curl для claim - все буде готово! 🎉**
