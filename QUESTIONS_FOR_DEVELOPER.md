# ❓ Питання для розробника soldrip.io

Привіт! Для автоматизації підключення гаманців потрібна інформація про API.

## 🔌 Про підключення гаманців:

### 1. Як працює підключення на сайті?
- [ ] Через кнопку "Connect Wallet" (Phantom/Solflare popup)
- [ ] Через API endpoint (простий POST запит)
- [ ] Інший спосіб? (який?)

### 2. Чи є API для підключення гаманця програмно?

**Якщо ТАК:**
- URL endpoint: `_______________________`
- HTTP метод: `_______________________`
- Потрібні параметри:
  ```json
  {
    "wallet_address": "...",
    "інші_поля": "..."
  }
  ```

### 3. Чи потрібна верифікація власності гаманця?
- [ ] Ні, просто адреса
- [ ] Так, потрібен підпис (signature)
- [ ] Так, потрібна транзакція

**Якщо потрібен підпис:**
- Яке повідомлення підписувати? `_______________________`
- Формат підпису: `_______________________`

### 4. API для claim:

- URL endpoint: `_______________________`
- HTTP метод: `_______________________`
- Потрібні параметри:
  ```json
  {
    "wallet_address": "...",
    "інші_поля": "..."
  }
  ```

### 5. API для перевірки балансу:

- URL endpoint: `_______________________`
- Приклад: `GET /api/wallet/balance/{address}`

### 6. Rate Limiting:

- Є обмеження на кількість запитів?
- Скільки запитів/хвилину можна робити?
- Чи потрібна затримка між підключеннями?

### 7. Формат відповідей:

**Успішне підключення:**
```json
{
  "success": true,
  "balance": 0.0
}
```

**Помилка:**
```json
{
  "success": false,
  "error": "..."
}
```

### 8. Authentication/Headers:

Чи потрібні спеціальні headers?
- [ ] API Key
- [ ] Bearer token
- [ ] Нічого не потрібно

---

## 📝 Приклади запитів

Будь ласка, надай приклади curl команд:

### Підключення:
```bash
curl -X POST https://soldrip.io/api/??? \
  -H "Content-Type: application/json" \
  -d '{"wallet_address":"TEST123"}'
```

### Claim:
```bash
curl -X POST https://soldrip.io/api/??? \
  -H "Content-Type: application/json" \
  -d '{"wallet_address":"TEST123"}'
```

### Баланс:
```bash
curl https://soldrip.io/api/???/TEST123
```

---

## 🧪 Тестування

Чи можна протестувати API на dev/staging середовищі перед продом?

---

**Дякую! Після отримання цієї інформації зможемо швидко налаштувати автоматизацію! 🚀**
