# 🚀 SOLdrip Automation Tool

Автоматизація роботи з [soldrip.io](https://soldrip.io) - генерація Solana гаманців, підключення та автоматичний claim.

## 📋 Можливості

- ✅ **Генерація гаманців** - створення необмеженої кількості Solana гаманців
- ✅ **Зберігання в CSV** - всі гаманці зберігаються в зручному форматі
- ✅ **Автоматичне підключення** - масове підключення до soldrip.io
- ✅ **Auto-claim** - автоматичний збір SOL з усіх гаманців
- ✅ **Статистика** - відстеження балансів та прогнози доходу

## 🛠️ Встановлення

### 1. Встановіть Rust

**Windows:**
```bash
# Завантажте та запустіть rustup-init.exe з:
https://rustup.rs/
```

**Linux/macOS:**
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

Перевірте встановлення:
```bash
rustc --version
cargo --version
```

### 2. Збудуйте проект

```bash
cd testdrip
cargo build --release
```

## 🚀 Використання

Запустіть програму:
```bash
cargo run --release
```

### Меню опцій:

```
1. Генерувати нові гаманці     - Створює N нових Solana гаманців
2. Підключити гаманці          - Підключає всі гаманці до SOLdrip
3. Зробити claim               - Збирає SOL з усіх підключених гаманців
4. Показати статистику         - Відображає поточну статистику
5. Вийти                       - Закриває програму
```

## 📊 Структура CSV файлу

Файл `wallets.csv` містить:
- **address** - публічна адреса гаманця
- **private_key** - приватний ключ (зберігайте в безпеці!)
- **status** - статус (pending, connected, failed)
- **balance** - поточний баланс
- **created_at** - дата створення
- **last_claim** - дата останнього claim

## 🔧 Налаштування для soldrip.io

**ВАЖЛИВО:** Перед використанням потрібно адаптувати код під API вашого друга.

### Що потрібно дізнатися:

1. **API Endpoints:**
   - Endpoint для підключення гаманця
   - Endpoint для claim
   - Endpoint для перевірки балансу

2. **Формат запитів:**
   - Які параметри потрібні для підключення?
   - Чи потрібен підпис транзакції?
   - Формат відповідей API

### Де змінювати код:

Відкрийте файл [`src/soldrip.rs`](src/soldrip.rs) та оновіть:

1. **Функція `connect_via_api`** (рядок ~50):
   ```rust
   let response = client
       .post(format!("{}/api/connect", SOLDRIP_URL))
       .json(&serde_json::json!({
           "wallet_address": wallet.address,
           // Додайте необхідні поля
       }))
       .send()
       .await?;
   ```

2. **Функція `claim`** (рядок ~70):
   ```rust
   let response = client
       .post(format!("{}/api/claim", SOLDRIP_URL))
       .json(&serde_json::json!({
           "wallet_address": wallet.address,
           // Додайте необхідні поля
       }))
       .send()
       .await?;
   ```

## 💡 Приклад використання

### Сценарій: Генерація 1000 гаманців

```bash
$ cargo run --release

🚀 SOLdrip Automation Tool
==================================================

Оберіть дію:
1. Генерувати нові гаманці
2. Підключити гаманці до SOLdrip
3. Зробити claim для всіх гаманців
4. Показати статистику
5. Вийти

Ваш вибір: 1
Скільки гаманців згенерувати? 1000

🔐 Генеруємо 1000 гаманців...
[████████████████████] 1000/1000 (00:02)
✅ Готово!

✨ Успішно згеновано 1000 гаманців!
📄 Збережено у файл: wallets.csv
```

### Очікуваний дохід:

- **1000 гаманців** × 0.15 SOL/місяць = **150 SOL/місяць**
- При ціні $100/SOL = **$15,000/місяць**

## 🔒 Безпека

⚠️ **ВАЖЛИВО:**
- Файл `wallets.csv` містить приватні ключі
- **НЕ ДІЛІТЬСЯ** цим файлом ні з ким
- Зберігайте резервні копії в безпечному місці
- Використовуйте шифрування для довгострокового зберігання

## 🐛 Дебаг режим

Для детального логування:
```bash
RUST_LOG=debug cargo run --release
```

## 📝 TODO

- [ ] Додати multithreading для швидшої генерації
- [ ] РеалізуватиProxy support для обходу rate limits
- [ ] Додати GUI інтерфейс
- [ ] Telegram бот для моніторингу
- [ ] Automatic retry при помилках
- [ ] Export статистики в Excel

## 🤝 Вклад

Створено для тестування та розробки soldrip.io

## 📄 Ліцензія

MIT License - використовуйте як хочете!

---

**Питання?** Зверніться до розробника soldrip.io для деталей API.
