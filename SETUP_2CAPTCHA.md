# 🤖 Налаштування 2Captcha - Покрокова інструкція

## Крок 1: Реєстрація на 2Captcha (5 хвилин)

### 1.1 Відкрийте сайт
👉 https://2captcha.com/?from=15836410

### 1.2 Натисніть "Sign Up"
- Введіть email
- Придумайте пароль
- Підтвердіть email

### 1.3 Увійдіть в акаунт
Ви побачите Dashboard з балансом $0.00

---

## Крок 2: Поповнення балансу (2 хвилини)

### 2.1 Натисніть "Add Funds"

### 2.2 Оберіть спосіб оплати:
- 💳 Visa/Mastercard
- 🪙 Криптовалюта (BTC, ETH, USDT)
- 💰 PayPal
- 🏦 Webmoney

### 2.3 Поповніть рахунок

**Рекомендовано для старту: $10-20**

Розрахунок:
```
1000 капч = ~$0.50-1.00
$10 = ~10,000-20,000 капч

Якщо claim 1 раз/день:
1000 гаманців × 30 днів = 30,000 капч/міс = ~$15-30/міс
```

---

## Крок 3: Отримання API Key (1 хвилина)

### 3.1 У Dashboard знайдіть розділ "API Key"

### 3.2 Скопіюйте ваш API key
Виглядає так:
```
f4a8b2c9d7e6f1a3b5c8d9e2f7a1b4c6d8e3f9a2b5c7d1e4f8a3b6c9d2e5f7a1
```

---

## Крок 4: Налаштування проекту (2 хвилини)

### 4.1 Створіть файл `.env` в папці проекту

```bash
# Windows (PowerShell)
Copy-Item .env.example .env

# Linux/macOS
cp .env.example .env
```

### 4.2 Відкрийте `.env` в текстовому редакторі

```bash
# Windows
notepad .env

# Linux/macOS
nano .env
# або
code .env
```

### 4.3 Вставте ваш API key

```env
# Замініть це:
CAPTCHA_API_KEY=ваш_api_key_тут

# На ваш справжній ключ:
CAPTCHA_API_KEY=f4a8b2c9d7e6f1a3b5c8d9e2f7a1b4c6d8e3f9a2b5c7d1e4f8a3b6c9d2e5f7a1
```

### 4.4 Збережіть файл

---

## Крок 5: Запуск проекту (1 хвилина)

### 5.1 Збудуйте проект

```bash
cargo build --release
```

Перший build займе 5-10 хвилин (завантажує всі залежності)

### 5.2 Запустіть програму

```bash
cargo run --release
```

---

## 🧪 Тестування (рекомендовано!)

### Перевірте що все працює з 1 гаманцем:

```bash
cargo run --release
```

**Меню:**
```
1. Генерувати гаманці
   → Введіть: 1

2. Підключити до SOLdrip
   → Побачите: ✅ Гаманець успішно підключено!

3. Зробити claim
   → Побачите:
     💳 Баланс 2Captcha: $10.00
     🤖 Використовую 2Captcha...
     ⏳ Чекаємо розв'язання (зазвичай 15-30 секунд)...
     ✅ Капча розв'язана за 20 секунд!
     💰 Виконуємо claim...
     ✅ Успішно claimed 0.00005 SOL

4. Показати статистику
```

---

## ✅ Checklist готовності:

- [ ] Зареєстровано на 2Captcha.com
- [ ] Поповнено баланс ($10-20)
- [ ] Отримано API key
- [ ] Створено файл `.env`
- [ ] Вставлено API key у `.env`
- [ ] Виконано `cargo build --release`
- [ ] Протестовано на 1 гаманці
- [ ] Claim працює! 🎉

---

## 🚀 Масовий запуск

Після успішного тесту:

```bash
cargo run --release

# 1. Генеруємо 1000 гаманців
Оберіть: 1 → 1000

# 2. Підключаємо (займе ~20-30 хвилин)
Оберіть: 2

# 3. Claim для всіх (займе ~5-8 годин)
Оберіть: 3
```

**Час розрахунок для claim 1000 гаманців:**
```
1000 гаманців × 20 секунд (капча) = 20,000 сек = ~5.5 годин
```

💡 **Порада:** Запустіть вночі або використайте screen/tmux

---

## 📊 Моніторинг балансу

### Перевірити баланс 2Captcha:

1. Зайдіть на https://2captcha.com/
2. Dashboard → Balance
3. Або програма показує баланс при claim

### Алерти низького балансу:

Програма попередить якщо баланс < $0.50:
```
⚠️  УВАГА: Низький баланс! Поповніть на https://2captcha.com
```

---

## ❓ Troubleshooting

### Помилка: "CAPTCHA_API_KEY не знайдено"

**Рішення:**
1. Перевірте що файл `.env` існує
2. Перевірте що в `.env` є `CAPTCHA_API_KEY=...`
3. Перезапустіть програму

### Помилка: "2Captcha error: ERROR_WRONG_USER_KEY"

**Рішення:**
- API key неправильний
- Скопіюйте ключ знову з 2captcha.com/setting

### Помилка: "2Captcha error: ERROR_ZERO_BALANCE"

**Рішення:**
- Поповніть баланс на https://2captcha.com/

### Капча розв'язується повільно (>60 сек)

**Причини:**
- Багато навантаження на 2Captcha (пік часу)
- Складна капча

**Рішення:**
- Зачекайте або спробуйте пізніше
- Програма автоматично почекає до 150 секунд

---

## 💰 Оптимізація витрат

### Поради для економії:

1. **Claim рідше:**
   - Замість щодня → раз на 3 дні
   - 1000 × 10 claim/міс = 10,000 капч = $5-10/міс

2. **Попросити друга зробити whitelist:**
   - 0 капч потрібно
   - $0/міс витрат
   - Див. [FOR_DEVELOPER_CAPTCHA.md](FOR_DEVELOPER_CAPTCHA.md)

3. **Використати дешевші сервіси:**
   - CapMonster: ~$0.3-0.8/1000
   - AntiCaptcha: ~$0.5-1.5/1000

---

## 🎉 Готово!

Після налаштування все працює автоматично:

```
1. Генеруєте гаманці ✅
2. Підключаєте до SOLdrip ✅
3. 2Captcha розв'язує капчу автоматично 🤖
4. Claim виконується автоматично ✅
5. Profit! 💰
```

**При доході 150 SOL/міс ($15,000) витрати на капчу ($25) = 0.17%**

**Вперед до прибутку! 🚀💰**
