# 🤖 Рішення проблеми капчі для claim

## Проблема

Для claim на soldrip.io потрібна **reCAPTCHA v3** - це означає, що кожен claim вимагає розв'язання капчі.

```json
{
  "captchaToken": "0cAFcWeA50JS1OzX2fe0V0Jdq0NLLATHGlKVlNz1uHMXRML..."
}
```

---

## 💡 Рішення 1: 2Captcha / AntiCaptcha (РЕКОМЕНДОВАНО)

**Найпростіший і найнадійніший спосіб** - використати сервіси автоматичного розв'язання капчі.

### Як це працює:

1. Ваш код надсилає капчу на сервіс
2. Сервіс розв'язує капчу (реальні люди або AI)
3. Отримуєте токен
4. Використовуєте токен для claim

### Вартість:

- **2Captcha**: ~$0.5-1 за 1000 капч
- **AntiCaptcha**: ~$0.5-1.5 за 1000 капч
- **CapMonster**: ~$0.3-0.8 за 1000 капч

### Розрахунок для 1000 гаманців:

Якщо claim 1 раз на день:
- 1000 claim/день × 30 днів = 30,000 капч/місяць
- Вартість: ~$15-30/місяць

**При доході 150 SOL ($15,000) - це лише 0.2% витрат!**

### Імплементація з 2Captcha:

```rust
// Додайте в Cargo.toml:
// two-captcha = "0.2"

use two_captcha::TwoCaptcha;

async fn solve_captcha(site_key: &str) -> Result<String> {
    let api_key = "ваш_2captcha_api_key";
    let solver = TwoCaptcha::new(api_key);

    let captcha_token = solver
        .recaptcha_v3("https://soldrip.io", site_key, 0.3)
        .await?;

    Ok(captcha_token)
}

// Використання:
let captcha_token = solve_captcha("site_key_від_друга").await?;
let amount = soldrip::claim_with_captcha(&wallet, &captcha_token).await?;
```

**Реєстрація:**
- 2Captcha: https://2captcha.com/
- AntiCaptcha: https://anti-captcha.com/
- CapMonster: https://capmonster.cloud/

---

## 💡 Рішення 2: Попросити друга додати API bypass

Оскільки це сайт вашого друга, найпростіше рішення:

### Варіант A: Whitelist для ваших гаманців

```javascript
// Backend код (Node.js приклад)
const WHITELISTED_ADDRESSES = [
  "адреса1...",
  "адреса2...",
  // ... ваші 1000 адрес
];

app.post('/api/claim', async (req, res) => {
  const { walletAddress, captchaToken } = req.body;

  // Skip captcha для whitelist
  if (WHITELISTED_ADDRESSES.includes(walletAddress)) {
    // Process claim без перевірки капчі
    return processClaim(walletAddress);
  }

  // Для інших - перевіряємо капчу
  await verifyCaptcha(captchaToken);
  return processClaim(walletAddress);
});
```

### Варіант B: Спеціальний API key для автоматизації

```javascript
app.post('/api/claim', async (req, res) => {
  const { apiKey, walletAddress } = req.body;

  // Спеціальний API key для автоматизації
  if (apiKey === process.env.AUTOMATION_API_KEY) {
    // Skip captcha
    return processClaim(walletAddress);
  }

  // Для звичайних користувачів - капча
  await verifyCaptcha(req.body.captchaToken);
  return processClaim(walletAddress);
});
```

**Переваги:**
- ✅ Безкоштовно
- ✅ Миттєво
- ✅ Надійно
- ✅ Повний контроль

---

## 💡 Рішення 3: Браузерна автоматизація

**Складніше, але можливо** - використати headless browser з розширеннями для автоматичного розв'язання капчі.

### Інструменти:
- Playwright + CapSolver extension
- Puppeteer + 2Captcha extension

**Не рекомендую** - ненадійно і повільно.

---

## 🎯 Рекомендована стратегія:

### Для вас (користувач):

**Короткостроково (зараз):**
```
1. Попросіть друга додати whitelist для ваших адрес
   АБО
2. Додати спеціальний API key для вашої автоматизації
```

**Довгостроково (якщо whitelist неможливий):**
```
1. Зареєструйтесь на 2Captcha.com
2. Поповніть баланс на $10-20
3. Інтегруйте в код (5 хвилин)
4. Profit! 💰
```

### Для друга (розробник):

**Найкраще рішення** - додати окремий endpoint:

```javascript
// /api/automation/claim - БЕЗ капчі, але з API key
app.post('/api/automation/claim', requireApiKey, async (req, res) => {
  const { walletAddress } = req.body;

  // Rate limiting по API key (наприклад, 100 req/min)
  await checkRateLimit(req.apiKey);

  return processClaim(walletAddress);
});
```

Це дозволить:
- ✅ Захистити від ботів (через API key)
- ✅ Контролювати rate limiting
- ✅ Дати можливість автоматизації без капчі
- ✅ Відстежувати автоматизовані claim

---

## 📊 Порівняння рішень:

| Рішення | Вартість | Швидкість | Складність | Надійність |
|---------|----------|-----------|------------|------------|
| 2Captcha | $15-30/міс | Середня (5-15s) | Низька ⭐ | Висока ⭐⭐⭐ |
| Whitelist | Безкоштовно | Миттєво | Низька ⭐ | Дуже висока ⭐⭐⭐⭐ |
| API Key | Безкоштовно | Миттєво | Низька ⭐ | Дуже висока ⭐⭐⭐⭐ |
| Browser | Безкоштовно | Повільна (20-30s) | Висока ⭐⭐⭐ | Низька ⭐ |

---

## 🚀 Що робити зараз:

### План дій:

1. **Зв'яжіться з другом** і покажіть цей документ
2. **Запропонуйте додати whitelist** для ваших адрес АБО API key
3. **Якщо друг не може** - реєструйтесь на 2Captcha
4. **Я оновлю код** після вашого вибору рішення

---

## 🔧 Код готовий для всіх варіантів:

### Варіант 1: З 2Captcha
```rust
// Додайте залежність і я оновлю код
```

### Варіант 2: З whitelist/API key
```rust
// Вже готово! Просто треба знати API key від друга
```

### Варіант 3: Без капчі
```rust
// Якщо друг видалить капчу для ваших адрес - все працюватиме як є
```

---

**Який варіант обираєте? 🤔**

1. Попросити друга додати whitelist/API key (безкоштовно, швидко)
2. Використати 2Captcha (платно $15-30/міс, але автономно)
3. Інший варіант?
